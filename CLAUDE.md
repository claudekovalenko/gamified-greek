# Working with this user

**Be brief.** Short answers, numbered steps, no long explanations. They have
asked for this explicitly. Do not pad replies with rationale, caveats or
recaps — state what was done or what to do, and stop.

## This repo

The content is not edited here. Words come from `going-deeper-greek-vocab`
(`js/data.js`) and concepts from `going-deeper-greek-concepts`
(`data/concepts.json`); `tools/sync.mjs` turns both into `data/content.json`.
Fix wording in the source app, then sync.
